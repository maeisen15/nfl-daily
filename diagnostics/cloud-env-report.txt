Cloud environment diagnostic report
Generated: 2026-07-26 (scheduled diagnostic run)

1) Python / pip versions
-------------------------
Python 3.11.15
pip 24.0 from /usr/lib/python3/dist-packages/pip (python 3.11)

2) TWITTERAPI_IO_KEY
-------------------------
Character count: 37 (value not reported per instructions)

3) Outbound HTTPS connectivity checks
-------------------------
https://www.espn.com          -> BLOCKED: gateway answered 403 to CONNECT (policy denial or upstream failure)
https://www.nytimes.com       -> BLOCKED: gateway answered 403 to CONNECT (policy denial or upstream failure)
https://api.twitterapi.io     -> BLOCKED: gateway answered 403 to CONNECT (policy denial or upstream failure)
https://russellstreetreport.com -> BLOCKED: gateway answered 403 to CONNECT (policy denial or upstream failure)

All four requests were intercepted by the environment's outbound agent proxy
(127.0.0.1, port from $HTTPS_PROXY) and rejected at the CONNECT stage before
reaching the target host. Confirmed via `$HTTPS_PROXY/__agentproxy/status`,
which logged all four as "connect_rejected" / "gateway answered 403 to
CONNECT (policy denial or upstream failure)". This indicates the session's
configured network policy does not allow outbound access to these hosts
(none of espn.com, nytimes.com, api.twitterapi.io, or russellstreetreport.com
are in the proxy's no_proxy/allowlist). This is an environment/network-policy
condition, not a DNS or target-server issue.

4) pip install feedparser pyyaml requests
-------------------------
Failed to build sgmllib3k
ERROR: Could not build wheels for sgmllib3k, which is required to install pyproject.toml-based projects

(feedparser depends on sgmllib3k, which failed to build a wheel in this
environment — likely missing build toolchain for legacy sdist-only packages.
pyyaml and requests were not reported as failing in the tail output.)

5) git remote / status
-------------------------
origin  http://local_proxy@127.0.0.1:41729/git/maeisen15/nfl-daily (fetch)
origin  http://local_proxy@127.0.0.1:41729/git/maeisen15/nfl-daily (push)

HEAD detached at refs/heads/main
nothing to commit, working tree clean
